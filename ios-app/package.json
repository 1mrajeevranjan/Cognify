{
  "name": "cognify-ios",
  "version": "1.0.0",
  "description": "Cognify iOS app via Capacitor",
  "main": "index.js",
  "scripts": {
    "sync": "npx cap sync ios",
    "open": "npx cap open ios",
    "build": "npx cap sync ios && npx cap open ios",
    "copy": "npx cap copy ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
